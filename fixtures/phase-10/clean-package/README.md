# Phase 10 Clean Package Fixture

This local package is packed, installed into a new temporary directory, and
required by the Phase 10 source evaluator. It has no runtime dependencies and
does not contact a registry.
